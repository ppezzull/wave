# Flaviano — Compiler + Executer + Settler (P1)

Owns the full deterministic spine: TS compiler → two SwapVM opcodes → `EnsStrategyRouter` → Aqua settlement → fork deploy. Spine contract = [10-10-PLAYBOOK.md](../strategy/10-10-PLAYBOOK.md) §1.5.

| ☐ | Task | Person | Collab |
|---|---|---|---|
| ✅ | ~~🚧 **Unblock the build first — two things are missing.** (a) `npm install` inside `srcs/requirements/swap-vm/` (the Foundry remappings resolve through `node_modules/`; without it every import fails). (b) **Re-vendor `test/base/AquaStrategyBuilders.sol` from upstream** — the vendoring commit `99d2144` dropped it, and `test/base/AquaSwapVMTest.sol:19` imports it, so `forge build` fails outright. It is **1inch upstream code, not one of our spikes** (its consumer carries the Degensoft licence header and declares `contract AquaSwapVMTest is AquaStrategyBuilders`). Six Aqua test files depend on it.~~ **DONE** — `npm install` run (599 pkgs); file restored from upstream in `28dec82` (verified by cloning `release/1.1` and diffing the tree) | Flaviano | — |
| ✅ | ~~**Step zero**~~ — DONE: `forge build && forge test` green on **untouched** vendored upstream (`srcs/requirements/swap-vm/`). Nothing below is debuggable until this passes: a broken toolchain looks exactly like a broken opcode. Then read, in order: an XYC test at `-vvvv`, `MinRate.sol` (the guard template), `CoreInvariants.t.sol` (inherit, don't rewrite), `ProgramBuilder.sol`. Why each → [1INCH-RUNBOOK.md](../strategy/1INCH-RUNBOOK.md) §F0 | Flaviano | — |
| ☐ | `ast.ts` — typed AST for 9 block types + Zod-bounded DSL (`feed` resolves via compiler-owned Chainlink registry; no addresses from the LLM). Freeze as `specVersion: 1` | Flaviano | → Flavio (parse target), Pietro (fixtures) |
| ☐ | `EnsStrategyRouter.sol` (fresh, not spike) — Simulator + SwapVM + Aqua table; post-ship hook emits `StrategyDeployed(strategyId, programHash, ensNode)`. Freeze signature + export ABI JSON | Flaviano | → Flavio (`resolveVerify`), Pietro (subgraph mapping) |
| ☐ | **🎯 Live swap through Aqua on a STOCK program** (flat fee + `_xycSwapXD`, **no balance instruction** — Aqua manages balances) — deploy router on fork → `approve` + `aqua.ship()` with `useAquaInsteadOfSignature: true` → `quote()` → `swap()` from a plain EOA → assert both transfers + `Swapped` in logs → save the `-vvvv` trace. **This is where the 1inch bounty flips from "in progress" to "qualified"** — its three requirements (official contracts, on-chain transfers in the demo, sane git history) need no custom opcode. Everything after this raises the score on a base that already counts | Flaviano | — |
| ☐ | `canonical.ts` — enforce `Deadline→Concentrate→Decay→OracleGuard→InventorySkew→MakerFee→ProtocolFee→Curve→Salt`; produce AST move-arrow + unified diff | Flaviano | → Pietro (WOW card) |
| ☐ | **Settle the oracle-guard band: one-sided or two-sided?** The docs say both — §1.5 has `\|implied − oracle\| > maxDeviationBps` (two-sided), while [PITCH.md](../strategy/PITCH.md) and [SWAPVM-INTERNALS.md](../sponsors/1inch/SWAPVM-INTERNALS.md) say it refuses *maker-unfavourable* fills (one-sided). It bites because the guard is the OUTER wrapper and therefore reads `amountOut` **after** the skew penalty — and that penalty moves the implied price in the maker's favour, so a two-sided band can trip on the side that *helps* the maker. Decide **before** the ArgsBuilder byte layout; a one-sided reading is a flag in `mode` and costs nothing. Left open it resurfaces as inexplicable red liveness tests | Flaviano | — |
| ✅ | ~~Check whether `_decayXD` reads an oracle~~ — **RESOLVED against source.** It does not: `_decayXD` parses exactly one argument, `period`. The only file in `src/instructions/` calling `latestRoundData()` is `OraclePriceAdjuster.sol`. The workshop presenter (19:38–20:37) was simplifying. Q&A answer stands clean: **decay is a pricing mechanism that moves price toward a target over time; `_oracleGuard2D` is a refusal that halts the swap** | Flaviano | Q&A armour |
| ☐ | ⚠️ **Compiler constraint found while checking the above** — `Decay.sol:81–83` documents a **quote/swap divergence**: in quote mode the instruction reads last-update time, and *"makers MUST NOT use backward jumps to this instruction as it breaks numerical consistency between quote and swap."* Our canonical order includes `Decay`, so the compiler must never emit a backward jump targeting it. In no other doc | Flaviano | → compiler rules |
| ☐ | Clean rewrite `InventorySkew.sol` + `OracleGuard.sol` (per §1.5, not spike). Guard = `internal view`, staleness check first branch + always reverts. Append both opcodes at END (slot 0 reserved). SPDX headers on every new `.sol` | Flaviano | — |
| ☐ | `rules.ts` as rules-as-data `{predicate, message, rewrite}`; stub all 6, implement `OracleGuardMustPrecedeSkew` + `ProtocolFeeLeMakerFee` first | Flaviano | — |
| ☐ | `MockOracle.sol` (in `test/mocks/`) + `OracleGuardStaleHalt.t.sol` + `OracleGuardClamp.t.sol` (band containment, kink monotonicity, maker-favored rounding, exact selectors) | Flaviano | — |
| ☐ | `ir.ts` + `emit.ts` — `[opcode:1][argsLength:1][args]`, byte-identical, deterministic. **TS-direct emit is primary; on-chain factory demoted to post-G2 stretch** | Flaviano | — |
| ☐ | `slots.json` self-check — forge script dumps opcode-index map; snapshot tests on both Solidity + TS sides (drift fails here, not G2). **G1 → merge to `main`** | Flaviano | gate |
| ☐ | `InventorySkewLiveness.t.sol` (penalty cap <100%⇒never bricks) + `InventorySkewAdditivity.t.sol` (subadditive-or-equal over size grid) | Flaviano | — |
| ☐ | Disassembler + round-trip test `decode(emit(ir))===ir`; `programHash()` = keccak256 of emitted bytes, wired into ship path/`StrategyDeployed` | Flaviano | → Pietro (bytecode pane), Flavio (hash-verify input) |
| ☐ | Take Pietro's subgraph handoff; `make deploy-swap-vm-aqua` on the fork; `graph deploy` against Pietro's graph-node; fire fixture swap; verify real `Swapped` entity queryable | Flaviano | ← Pietro (schema/mapping); → Flavio + Pietro (endpoint URL) |
| ☐ | Mutation harness `MUTATION=M1|M2|M3 forge test` (M1 drops staleness revert, M2 flips clamp, M3 penalty >1); capture RED | Flaviano | → Pietro (RED/GREEN proof) |
| ☐ | Deploy `MockAggregatorV3` + deviation/restore control script | Flaviano | → Pietro (Beat B arming — does not slide) |
| ☐ | Pair until autonomous retune fires zero-click through router (Flavio `graphDelta`→`recompileAndShip()`→`dock()`/`ship()`); re-read 5 deleted spikes vs rewrites, confirm zero copy. **G2 → merge to `main`** | Flaviano | ← Flavio (retune); gate |
| ☐ | Commit swap-trace artifact: one trace showing `IAqua` pull/push + both opcodes + `Swapped` (1inch 30s proof) | Flaviano | — |
| ☐ | Gas snapshot ≤5%, `quote()==swap()` consistency test, `forge test` clean on `ci` profile. **G3 → freeze + merge to `main`** | Flaviano | gate |
| ☐ | Record RED/GREEN mutation split-screen; rehearse the Sepolia demo path (live `ship()`/`swap()`, backup-RPC + second-funded-wallet swap on a failed beat while Pietro narrates) | Flaviano | demo with Pietro |

**Never cut:** the live `swap()` through Aqua, the `_oracleGuard2D` halt, byte-identical compiler emit.
**Cut floor:** `canonical.ts` + rules 1&2 + TS-direct emit + `OracleGuardStaleHalt`/`Clamp` + M1/M2 toggles + one RED screenshot; skew keeps penalty path only.
**Dealbreaker:** the live `swap()` through Aqua on Sepolia (pull/push in trace, `Swapped` indexed by the subgraph, both opcodes hot) + a working `_oracleGuard2D` halt. Absent Sunday → 1inch is gone and finalist Practicality/WOW has no engine.
**Demo/Q&A:** drive the live Sepolia demo (real `ship()`/`swap()`, swap to backup RPC + second funded wallet on a failed beat while Pietro narrates — no fork to re-cut, no canned twin). Own Q&A on opcode math + maker-favored rounding (exactIn floors, exactOut `ceilDiv`), "why a native opcode vs `_extruction`?" (cite `Extruction.sol`'s own header warning — never "impossible"), and license compliance. Sunday: finalist judging session with Pietro; you confirm the Sepolia seed + subgraph sync before judging.
