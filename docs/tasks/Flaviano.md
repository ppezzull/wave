# Flaviano — Compiler + Executer + Settler (P1)

Owns the full deterministic spine: TS compiler → two SwapVM opcodes → `EnsStrategyRouter` → Aqua settlement → Sepolia deploy. Spine contract = [10-10-PLAYBOOK.md](../strategy/10-10-PLAYBOOK.md) §1.5.

| ☐ | Task | Person | Collab |
|---|---|---|---|
| ✅ | ~~🚧 **Unblock the build first — two things are missing.** (a) `npm install` inside `srcs/requirements/swap-vm/` (the Foundry remappings resolve through `node_modules/`; without it every import fails). (b) **Re-vendor `test/base/AquaStrategyBuilders.sol` from upstream** — the vendoring commit `99d2144` dropped it, and `test/base/AquaSwapVMTest.sol:19` imports it, so `forge build` fails outright. It is **1inch upstream code, not one of our spikes** (its consumer carries the Degensoft licence header and declares `contract AquaSwapVMTest is AquaStrategyBuilders`). Six Aqua test files depend on it.~~ **DONE** — `npm install` run (599 pkgs); file restored from upstream in `28dec82` (verified by cloning `release/1.1` and diffing the tree) | Flaviano | — |
| ✅ | ~~**Step zero** — `forge build && forge test` green on **untouched** vendored upstream.~~ **DONE: 85 suites, 706 tests passed, 0 failed, 844ms.** A broken toolchain looks exactly like a broken opcode, so nothing below was debuggable until this passed. ✅ **Readings done (Jul 25)** — XYC trace at `-vvvv` (settlement envelope: ecrecover → both transfers → `Swapped`; internal dispatch invisible to the tracer), `MinRate.sol` (cross-multiplication assert, `internal` non-view, `ArgsBuilder` pattern), `CoreInvariants.t.sol` (inherit `assertAllInvariantsWithConfig`), `ProgramBuilder.sol` (pointer-identity index resolution). Why each → [1INCH-RUNBOOK.md](../strategy/1INCH-RUNBOOK.md) §F0 | Flaviano | — |
| ☐ | `ast.ts` — typed AST for 9 block types + Zod-bounded DSL (`feed` resolves via compiler-owned Chainlink registry; no addresses from the LLM). Freeze as `specVersion: 1` | Flaviano | → Flavio (parse target), Pietro (fixtures) |
| ✅ | ~~`EnsStrategyRouter.sol` (fresh, not spike) — Simulator + SwapVM + Aqua table; post-ship hook emits `StrategyDeployed(strategyId, programHash, ensNode)`. Freeze signature + export ABI JSON~~ **DONE** — ABI at `srcs/requirements/swap-vm/abi/EnsStrategyRouter.abi.json`. **Two frozen decisions (recorded here, not doc-sourced):** (a) **indexing** — `strategyId` + `ensNode` are `indexed` (subgraph filter keys), `programHash` is NOT (payload, never filtered on); (b) **placeholder** — `announceStrategy()` emits `programHash = bytes32(0)` until the compiler's `programHash()` is wired (riga 22); **Pietro's mapping must tolerate `programHash == 0`** until then; (c) **access control** — `announceStrategy()` is `onlyOwner` (the announcer key set at construction via `Rescuable`). Without it anyone could inject strategies that never shipped into the reputation feed. Verified ABI-neutral: re-exporting after the change produced a byte-identical ABI (the `OwnableUnauthorizedAccount` error was already present via `rescueFunds`), so this is **not** a re-freeze — consumers need no regeneration. Covered by `test/EnsStrategyRouter.t.sol` (3 tests: event shape, zero-placeholder, non-owner revert). ⚠️ **COORDINATION ITEM WITH FLAVIO — can break Beat B at demo time, not compile time.** The Sepolia router's owner is the deployer EOA `0x2058C253029bB0Cf1E1aD43DfAEF63D658A8dddf`. If Flavio's agent announces from any other key, `announceStrategy` reverts on stage. Resolve before the demo by either (a) giving the agent that key, or (b) `router.transferOwnership(<agent key>)` — `Ownable` ships it, so it's a runbook line, not code. Confirm which key his announce path uses | Flaviano | → Flavio (`resolveVerify`), Pietro (subgraph mapping) |
| ✅ | ~~**🎯 Live swap through Aqua on a STOCK program** (flat fee + `_xycSwapXD`, **no balance instruction** — Aqua manages balances)~~ **DONE on Sepolia, Jul 25 2026 — the 1inch bounty is QUALIFIED.** Both legs proved: anvil first (de-risk), then live Sepolia (chain 11155111). Self-deployed Aqua `0xdc8C2b141CF705F12f6BB45af3830341daBB1D1E` (763,281 gas); `EnsStrategyRouter` `0xeb513fd18c391fae1513ff12c1f97bf659d052c4` (5.89M gas); swap tx [`0xd8056fde…9601`](https://sepolia.etherscan.io/tx/0xd8056fdec5129c7d70a9fbb0b6e6f1721514f67269e3d043e04239b9d1099601) block 11350065, status 1. Receipt carries the two ERC-20 `Transfer`s, Aqua pull+push, and **`Swapped` emitted by our router** (topic0 `0x54bc5c02…`): 10e18 in → 9.066108938801491315e18 out, identical to `quote()` and to the anvil run. Total spend ≈0.017 ETH. Trace: `artifacts/sepolia-swap-trace.txt`. ⚠️ **Deploy-flow gotcha** — `DeployEnsStrategyRouter.s.sol` reads Aqua from `config/constants.json` (via `Config.readSwapVMRouterParameters`), NOT from `OPS_AQUA_ADDRESS`; the Makefile's `process-aqua-address` (Makefile:136) upserts the env var into that JSON first, and the script reverts on a zero slot. Verify `.aqua`/`.owner` for the chain id before deploying the router. Booth compliance question on Aqua self-deploy (PROD-TESTNET §0) still open | Flaviano | — |
| ☐ | `canonical.ts` — enforce `Deadline→Concentrate→Decay→OracleGuard→InventorySkew→MakerFee→ProtocolFee→Curve→Salt`; produce AST move-arrow + unified diff | Flaviano | → Pietro (WOW card) |
| ✅ | ~~**Settle the oracle-guard band: one-sided or two-sided?**~~ — **DECIDED: ONE-SIDED.** Halt only when the implied price is *unfavourable to the maker* relative to the oracle beyond `maxDeviationBps`; a maker-favourable price never trips the guard. The two-sided behaviour is reserved as an unimplemented `flags` bit (forward-compatible, no extra test surface now). Canonical: [10-10-PLAYBOOK.md](../strategy/10-10-PLAYBOOK.md) §1.5; reasoning: [1INCH-RUNBOOK.md](../strategy/1INCH-RUNBOOK.md) F0.6. Why one-sided: the guard is the OUTER wrapper, reads `amountOut` *after* the `_inventorySkew2D` penalty (which moves price in the maker's favour), so a two-sided band would reject fills good for the maker. The ArgsBuilder now reserves a `flags` bit; the `mode` byte keeps revert(0)/clamp(1) | Flaviano | — |
| ✅ | ~~Check whether `_decayXD` reads an oracle~~ — **RESOLVED against source.** It does not: `_decayXD` parses exactly one argument, `period`. The only file in `src/instructions/` calling `latestRoundData()` is `OraclePriceAdjuster.sol`. The workshop presenter (19:38–20:37) was simplifying. Q&A answer stands clean: **decay is a pricing mechanism that moves price toward a target over time; `_oracleGuard2D` is a refusal that halts the swap** | Flaviano | Q&A armour |
| ☐ | ⚠️ **Compiler constraint found while checking the above** — `Decay.sol:81–83` documents a **quote/swap divergence**: in quote mode the instruction reads last-update time, and *"makers MUST NOT use backward jumps to this instruction as it breaks numerical consistency between quote and swap."* Our canonical order includes `Decay`, so the compiler must never emit a backward jump targeting it. In no other doc | Flaviano | → compiler rules |
| ☐ | Clean rewrite `InventorySkew.sol` + `OracleGuard.sol` (per §1.5, not spike). Guard = **`internal` (NON `view` — `ctx.runLoop()` is non-view so a `view` guard won't compile; static-safe under `quote()` via runtime `isStaticContext`, model `MinRate._requireMinRate1D`)**, staleness check first branch + always reverts. Append both opcodes at END (slot 0 reserved). SPDX headers on every new `.sol` | Flaviano | — |
| ☐ | `rules.ts` as rules-as-data `{predicate, message, rewrite}`; stub all 6, implement `OracleGuardMustPrecedeSkew` + `ProtocolFeeLeMakerFee` first | Flaviano | — |
| ☐ | `MockOracle.sol` (in `test/mocks/`) + `OracleGuardStaleHalt.t.sol` + `OracleGuardClamp.t.sol` (band containment, kink monotonicity, maker-favored rounding, exact selectors) | Flaviano | — |
| ☐ | `ir.ts` + `emit.ts` — `[opcode:1][argsLength:1][args]`, byte-identical, deterministic. **TS-direct emit is primary; on-chain factory demoted to post-G2 stretch** | Flaviano | — |
| ☐ | `slots.json` self-check — forge script dumps opcode-index map; snapshot tests on both Solidity + TS sides (drift fails here, not G2). **G1 → merge to `main`** | Flaviano | gate |
| ☐ | `InventorySkewLiveness.t.sol` (penalty cap <100%⇒never bricks) + `InventorySkewAdditivity.t.sol` (subadditive-or-equal over size grid) | Flaviano | — |
| ☐ | Disassembler + round-trip test `decode(emit(ir))===ir`; `programHash()` = keccak256 of emitted bytes, wired into ship path/`StrategyDeployed` | Flaviano | → Pietro (bytecode pane), Flavio (hash-verify input) |
| ☐ | Take Pietro's subgraph handoff; `make deploy-swap-vm-aqua` on Sepolia; `graph deploy` against Pietro's graph-node; fire fixture swap; verify real `Swapped` entity queryable | Flaviano | ← Pietro (schema/mapping); → Flavio + Pietro (endpoint URL) |
| ☐ | Mutation harness `MUTATION=M1|M2|M3 forge test` (M1 drops staleness revert, M2 flips clamp, M3 penalty >1); capture RED | Flaviano | → Pietro (RED/GREEN proof) |
| ☐ | Deploy `MockAggregatorV3` + deviation/restore control script | Flaviano | → Pietro (Beat B arming — does not slide) |
| ☐ | Pair until autonomous retune fires zero-click through router (Flavio `graphDelta`→`recompileAndShip()`→`dock()`/`ship()`); re-read 5 deleted spikes vs rewrites, confirm zero copy. **G2 → merge to `main`** | Flaviano | ← Flavio (retune); gate |
| ☐ | Commit swap-trace artifact: one trace showing `IAqua` pull/push + both opcodes + `Swapped` (1inch 30s proof) | Flaviano | — |
| ☐ | Gas snapshot ≤5%, `quote()==swap()` consistency test, `forge test` clean on `ci` profile. **G3 → freeze + merge to `main`** | Flaviano | gate |
| ☐ | Record RED/GREEN mutation split-screen; rehearse the Sepolia demo path (live `ship()`/`swap()`, backup-RPC + second-funded-wallet swap on a failed beat while Pietro narrates) | Flaviano | demo with Pietro |

**Never cut:** the live `swap()` through Aqua, the `_oracleGuard2D` halt, byte-identical compiler emit.
**Cut floor:** `canonical.ts` + rules 1&2 + TS-direct emit + `OracleGuardStaleHalt`/`Clamp` + M1/M2 toggles + one RED screenshot; skew keeps penalty path only.
**Dealbreaker:** the live `swap()` through Aqua on Sepolia (pull/push in trace, `Swapped` indexed by the subgraph, both opcodes hot) + a working `_oracleGuard2D` halt. Absent Sunday → 1inch is gone and finalist Practicality/WOW has no engine.
**Demo/Q&A:** drive the live Sepolia demo (real `ship()`/`swap()`, swap to backup RPC + second funded wallet on a failed beat while Pietro narrates — no fork to re-cut, no canned twin). Own Q&A on opcode math + maker-favored rounding (exactIn floors, exactOut `ceilDiv`), "why a native opcode vs `_extruction`?" (cite `Extruction.sol`'s own header warning — never "impossible"), and license compliance. Sunday: finalist judging session with Pietro; you confirm the Sepolia seed + subgraph sync before judging.
