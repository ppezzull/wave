# ETHOnline 2026 - Second check-in (as of Thu Sep 10, 2026)

Copy each answer into its field.

---

**Project name**
wave

**Category**
DeFi

**Emoji**
🌊

**GitHub repository** *(fixes the placeholder from the draft)*
https://github.com/ppezzull/wave

**Short description** *(≤100 chars - 90)*
Post a strategy in plain English: it compiles to 1inch SwapVM bytecode and ships on-chain

**Any blockers? Need help?**
Nothing blocking the build: everything runs on a local Sepolia fork (anvil + graph-node
+ our agent). Two logistics items: (1) we need ~0.1 Sepolia ETH in the team wallet to
redeploy the new router + factory contracts on live Sepolia, faucets have been stingy;
(2) World ID Sandbox access is still pending (lead-time form), which gates the World
AgentKit testing on a real phone.

**Is there anything else you think we should know?**
Our colors and branding come from the Lisbon beach: before the hackathon we went for a
swim in the sea and fell in love with the sea green to blue gradient, so the whole UI
wears it.

**Will you join Live Judging for Top 10?**
Yes

**On track for submitting?**
Yes

**Prizes you're going for**
The Graph - ($15000)
World - ($7000)
1inch - ($7000)

**Progress since last check-in**
- On-chain authorship: new StrategyFactory.attribute() contract + tests (onlyOwner,
  append-only), so every shipped strategy records WHO wrote it. The social layer
  previously had no author because the maker is the agent's EOA.
- Subgraph learned Strategy.author; the UI is fully de-stubbed: /u/<wallet> profiles
  with real stats from author-keyed queries, /chat threads = your shipped strategies,
  live SSE stream of the agent's autonomous retune actions (with dedup).
- Ship pipeline verified end-to-end on a Sepolia fork: announce, attribute, approve,
  dock on Aqua, then capital + author + description indexed. One strategy id everywhere
  (we found and fixed a subtle ABI-encoding mismatch that stranded liquidity under an
  unreachable hash).
- Composed and shipped strategies from the UI in natural language on the fork, with a
  human-in-the-loop confirm on the destructive action.
- All of the above is pushed to main as incremental commits (contract + subgraph,
  agent, UI).
Next: live Sepolia redeploy + subgraph v0.0.6 on Studio (funding-gated), World AgentKit
integration at the publish gate, 2-4 min demo video.
