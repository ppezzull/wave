{
  "name": "wave-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "wave autonomous retune agent — Mastra + z.ai + custom MCP (P2/Flavio). Policy module first.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
