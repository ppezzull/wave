# agent — viem + @1inch/aqua-sdk runner
# PLACEHOLDER (build kickoff Fri Jul 24). Owns the ENS resolve+verify half of
# Move #5 + the dock()/recompile/ship() retune loop driven by subgraph deltas.
FROM node:20-alpine

WORKDIR /app

# Real agent lands here during the build (P2):
# COPY package*.json ./
# RUN npm ci
# COPY . .

# Placeholder so the image builds today:
RUN echo '{"name":"strategy-compiler-agent","private":true}' > package.json

CMD ["echo", "agent placeholder — wire viem + aqua-sdk ship/dock/monitor (Move #5) during the build"]
