{
  "name": "wave-compiler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "wave deterministic strategy compiler — Zod AST → canonical order → IR → SwapVM bytecode (P1/Flaviano). ast.ts frozen at specVersion 1.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
