# dependencies & build artifacts
node_modules/
out/
cache/
artifacts/
coverage/
coverage.json
build/
.next/
dist/
generated/
.mastra/

# foundry run artifacts
broadcast/
lib/

# env (keep the example)
.env
.env.*
!.env.example

# os / editor
.DS_Store
*.log
.idea/
.vscode/

# MCP config — machine-specific paths, use .mcp.example.json as template
.mcp.json

# Mastra monitor evidence log (agent) — default ./evidence.jsonl
evidence*.jsonl
